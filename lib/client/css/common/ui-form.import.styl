form
  +ui-form-inputs()
    ui-base()
    padding 0 1em
    border border-secondary
    type-regular()
    color color-type
    text-shadow 0 0 0 rgba(color-white, 0)

    &.ui-small
      ui-small()
